# GitLab MCP Server Configuration
# Copy this file to .env for local testing (DO NOT COMMIT .env)

# GitLab Personal Access Token (required)
# Create at: GitLab → Settings → Access Tokens
# Scope: api
GITLAB_PERSONAL_ACCESS_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxxx

# GitLab API URL (optional, defaults to hs2git.ab-games.com)
# For GitLab.com use: https://gitlab.com/api/v4
# For self-hosted use: https://your-gitlab.com/api/v4
GITLAB_API_URL=https://gitlab.com/api/v4

# Token Header Type (optional, defaults to Authorization)
# Options: PRIVATE-TOKEN, JOB-TOKEN, or Authorization
GITLAB_TOKEN_HEADER=PRIVATE-TOKEN

# Example usage for local MCP server testing:
# 1. Copy this file: cp .env.example .env
# 2. Replace xxxxx with your actual GitLab PAT
# 3. Update GITLAB_API_URL if using self-hosted GitLab
# 4. Run: npm run mcp:serve
#
# WARNING: Never commit .env to version control!
